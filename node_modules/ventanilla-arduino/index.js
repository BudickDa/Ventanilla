//get Johnny Five
var five=require('johnny-five');

var actors = require('./actors');
var sensors = require('./sensors');

exports.init = function(configBoard, cb){
  var board = new five.Board(configBoard.port);
  board.on('ready',function(){
    return sensors.init(configBoard.sensors,board,five,cb);
  });
}
