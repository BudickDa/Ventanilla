exports.Add = function(block,cb){
  console.log("Will create Logic  - initilizing");
  var result = [];
  for(i in block.inputs){
    console.log("Add:");
    console.log(blocks[block.inputs[i].uid].output(this.value, block.inputs[i].type));
    result += blocks[block.inputs[i].uid].output(this.value, block.inputs[i].type);
  }
  console.log("Will create Logic  - sucessfull");
  return cb(result);
};
