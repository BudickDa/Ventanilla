exports.Add = function(block,blocks,cb){
  var result = [];
  for(i in block.inputs){
    console.log("Add:");
    console.log(blocks[block.inputs[i]].output(this.value));
    result += blocks[block.inputs[i]].output(this.value);
  }
  return cb(result);
};
